const React = require('react');

const createReactClass = require('create-react-class');

const Weather = createReactClass({
  render: function(){
      return (
        <h3>Great Weather Channel!</h3>
      );
  }
});

module.exports = Weather;

